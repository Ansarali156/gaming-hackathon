# ==========================================
# NODE.JS BACKEND - ENVIRONMENT VARIABLES
# ==========================================

# --- 1. CORE APPLICATION ---
BACKEND_PORT=4000
FRONTEND_URL="http://localhost:3000,https://ai-gaming-hackthon.vercel.app"
# CORS — comma-separated list of allowed origins (first is fallback)
BACKEND_URL="http://localhost:4000"
JWT_SECRET="your-jwt-secret-key"

# --- 2. DATABASE (RENDER POSTGRES) ---
DATABASE_URL="postgresql://user:password@host/dbname"

# --- 3. PAYMENTS (RAZORPAY) ---
# Use the same keys as the frontend
RAZORPAY_KEY_ID="rzp_test_..."
RAZORPAY_KEY_SECRET="your_secret_..."

# --- 6. SUN FORWARDING (LOCAL) ---
# Shared symmetric key used to encrypt and authenticate payloads sent to SUN
SUN_SHARED_KEY="replace_with_secure_random_key"
# Full URL to the SUN endpoint that will receive the encrypted JSON
SUN_ENDPOINT_URL="http://localhost/sun/public/gaminghackathon/create-order.php"

# --- 4. MAILER (NODEMAILER / GMAIL) ---
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"
EMAIL_FROM="noreply@incuxai.com"

# --- 5. DISCORD OAUTH (OPTIONAL) ---
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
